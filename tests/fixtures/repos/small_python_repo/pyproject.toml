[project]
name = "test-project"
version = "0.1.0"
description = "A test project"
requires-python = ">=3.10"
dependencies = [
    "flask>=2.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]
