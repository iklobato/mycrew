# OpenRouter - required for model API calls
OPENROUTER_API_KEY=your_openrouter_api_key

# Do NOT set OPENAI_BASE_URL when using OpenRouter; it can break provider routing.

# Or use OpenAI (set OPENAI_API_KEY; OPENROUTER_API_KEY takes precedence when both exist)
# OPENAI_API_KEY=your_openai_key
